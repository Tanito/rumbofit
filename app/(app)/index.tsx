import { useRouter } from 'expo-router'; import { Text, View } from 'react-native'; import { AuthButton } from '@/components/AuthButton'; import { Brand } from '@/components/Brand'; import { useAuth } from '@/context/AuthContext'; import { colors, fonts, spacing } from '@/theme';
export default function PrivateHome() { const { user, signOut } = useAuth(); const router = useRouter(); return <View style={{ flex: 1, backgroundColor: colors.warmWhite, alignItems: 'center', justifyContent: 'center', padding: spacing.xl, gap: spacing.lg }}><Brand compact /><Text style={{ color: colors.deepBlue, fontFamily: fonts.display, fontSize: 25, textAlign: 'center' }}>¡Bienvenido a Rumbo Fit!</Text><Text style={{ color: colors.muted, fontFamily: fonts.body, fontSize: 16 }}>{user?.email}</Text><Text style={{ color: colors.graphite, fontFamily: fonts.body, textAlign: 'center' }}>Esta es tu pantalla privada de prueba. El dashboard está en camino.</Text><View style={{ width: '100%', maxWidth: 300 }}><AuthButton title="Cerrar sesión" onPress={async () => { await signOut(); router.replace('/(auth)/login'); }} /></View></View>; }
