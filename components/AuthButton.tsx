import { ActivityIndicator, Pressable, Text } from 'react-native'; import { colors, fonts, radius } from '@/theme';
export function AuthButton({ title, onPress, loading, variant = 'primary' }: { title: string; onPress: () => void; loading?: boolean; variant?: 'primary' | 'google' | 'apple' }) { const primary = variant === 'primary'; return <Pressable onPress={onPress} disabled={loading} accessibilityRole="button" accessibilityLabel={title} style={({ pressed }) => ({ minHeight: 54, borderRadius: radius.sm, alignItems: 'center', justifyContent: 'center', backgroundColor: primary ? colors.orange : variant === 'apple' ? '#000' : colors.white, borderWidth: primary ? 0 : 1, borderColor: colors.border, opacity: pressed || loading ? 0.72 : 1 })}>{loading ? <ActivityIndicator color={primary || variant === 'apple' ? colors.white : colors.deepBlue} /> : <Text style={{ color: primary || variant === 'apple' ? colors.white : colors.graphite, fontFamily: fonts.medium, fontSize: 15 }}>{title}</Text>}</Pressable>; }
